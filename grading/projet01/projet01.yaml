- id: group_a3e4a6
- Makefile:           5 # max 5,
- bit.h: # NE PAS REMPLIR CETTE LIGNE
      typedef rot_dir_t: 1                   # max 1,
- bit.c: # NE PAS REMPLIR CETTE LIGNE
  - lsb4():              1           # max 1,
  - msb4():              1           # max 1,
  - lsb8():              1           # max 1,
  - msb8():              1           # max 1,
  - merge4():            1           # max 1,
  - merge8():            1           # max 1,
  - bit_get():           1           # max 1,
  - bit_set():           1           # max 1,
  - bit_unset():         1           # max 1,
  - bit_edit():          2           # max 2,
  - bit_rotate():        5               # max 5,
- alu.h: # NE PAS REMPLIR CETTE LIGNE
      alu_output_t_t:    3               # max 3,
- alu.c: # NE PAS REMPLIR CETTE LIGNE
  - get_flag():          5               # max 6, Beaucoup de copier-coller dans cette fonction
  - set_flag():          5               # max 6, Beaucoup de copier-coller dans cette fonction
  - alu_add8(): # NE PAS REMPLIR CETTE LIGNE
      vérification d'usage:  1        # max 1,
      modularisation:        5        # max 5,
      addition:              1        # max 1,
      calcul des flags:      3        # max 3,
      partie set_flags:      3        # max 3,
  - alu_sub8(): # NE PAS REMPLIR CETTE LIGNE
      soustraction:          1        # max 1,
      calcul des flags:      3        # max 3,
  - alu_add16_low(): # NE PAS REMPLIR CETTE LIGNE
      addition:              5        # max 5,
      calcul des flags:      1        # max 1,
  - alu_add16_high(): # NE PAS REMPLIR CETTE LIGNE
      addition:              2.5        # max 2.5,
      calcul des flags:      0.5        # max 0.5,
  - alu_shift():             6      # max 6,
  - alu_shiftR_A():          5      # max 5,
  - alu_rotate():            5      # max 5,
  - alu_carry_rotate():      2.5      # max 2.5,
- bus.h: # NE PAS REMPLIR CETTE LIGNE
      bus_t:                 3         # max 3,
- bus.c: # NE PAS REMPLIR CETTE LIGNE
  - bus_plug(): # NE PAS REMPLIR CETTE LIGNE
      vérification d'usage:            0    # max 1, Pas de vérification que bus n'est pas NULL avant d'y accéder
      vérification de tout le bus:     2.5  # max 2.5,
      modularisation:                  2.5  # max 2.5,
  - bus_forced_plug(): # NE PAS REMPLIR CETTE LIGNE
      vérifications d'usage:           1    # max 1,
      corps:                           2.5  # max 2.5,
      modularisation:                  2.5  # max 2.5,
  - bus_remap(): # NE PAS REMPLIR CETTE LIGNE
      vérifications d'usage:           2    # max 2.5, Pas de vérification que bus != NULL
      vérification du range:           2    # max 2,
      corps (boucle):                  2.5  # max 2.5,
  - bus_unplug():                    3 # max 3.5, Pas de vérification que bus != NULL
  - bus_read():                      3 # max 3.5, Pas de vérification que bus != NULL
  - bus_write():                     3 # max 3.5, Pas de vérification que bus != NULL
  - bus_read16():                    5.5 # max 6, Pas de vérification que bus != NULL
  - bus_write16():                   5.5 # max 6, Pas de vérification que bus != NULL
- component.h: # NE PAS REMPLIR CETTE LIGNE
      component_t:                   5        # max 5,
- component.c: # NE PAS REMPLIR CETTE LIGNE
  - component_create():              6 # max 6,
  - component_shared():              2.5 # max 3.5, Pas de vérification que c_old ni sa mémoire != NULL
  - component_free():                4 # max 5, Dans le cas où c->mem == NULL, start et end ne sont pas mis à 0
- memory.h: # NE PAS REMPLIR CETTE LIGNE
      addr_t:        1 # max 1,
      data_t:        1 # max 1,
      memory_t:      4 # max 4,
- memory.c: # NE PAS REMPLIR CETTE LIGNE
  - memory_create():              6 # max 6,
  - memory_free():                2 # max 2.5, Dans le cas où mem->memory == NULL, size n'est pas remis à 0
- gameboy.h: # NE PAS REMPLIR CETTE LIGNE
      gameboy_t:                  4        # max 5, Pas de champ pour stocker le nombre de composants actuellement connectés
- gameboy.c: # NE PAS REMPLIR CETTE LIGNE
  - gameboy_create(): # NE PAS REMPLIR CETTE LIGNE
      vérification d'usage:       0.5    # max 0.5,
      init de base:               2    # max 2,
      plug work-ram:              5    # max 5,
      plug echo-ram:              2.5    # max 2.5,
  - gameboy_free():               2  # max 5, L'echo ram n'est pas unplug, comme tous les autres components
- cpu.h: # NE PAS REMPLIR CETTE LIGNE
      typedef:           1 # max 1,
      une reg-pair:      5 # max 5,
      les autres:        3 # max 3,
      bus (pointeur):    2 # max 2,
      autres champs:     4 # max 4,
- cpu.c: # NE PAS REMPLIR CETTE LIGNE
  - cpu_init():          6               # max 6,
  - cpu_plug():          2               # max 2,
  - cpu_free():          2               # max 2, Pas besoin de mettre un return à la fin d'une fonction vide
  - cpu_cycle():         3               # max 3,
- cpu-storage.c: # NE PAS REMPLIR CETTE LIGNE
  - cpu_read_at_idx():                 6 # max 6,
  - cpu_read16_at_idx():               3 # max 3,
  - cpu_write_at_idx():                3.5 # max 3.5,
  - cpu_write16_at_idx():              1.5 # max 1.5,
  - cpu_SP_push():                     5 # max 6, Le SP est décrémenté même si il y a une erreur d'écriture
  - cpu_SP_pop():                      6 # max 6,
- cpu-register.c: # NE PAS REMPLIR CETTE LIGNE
  - cpu_reg_get():                     2.5    # max 2.5,
  - cpu_reg_pair_get():                2    # max 2,
  - cpu_reg_set():                     2.5    # max 2.5,
  - cpu_reg_pair_set():                5    # max 5,
- style:        1 # max 2, min -2, (normal = 0), Code très propre et commentaires doxygen
- bonus/malus:  1 # max 5, min -5, (normal = 0), Bonne modularisation du code
